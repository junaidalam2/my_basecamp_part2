<h1>New task for <%=@project&.name%></h1>

<table class="table table-striped table-bordered table-hover">
  <thead class="thead-dark">
    <tr>
      <th>Task Name</th>
      <th>Description</th>
      <th>Status</th>
      <th>Assigned to</th>
      <th>Create task</th>
    </tr>
  </thead>
  <tbody>

  <%= render "form", list: @list %>


  <tbody>
</table>

<br>

<div>
  <%#= link_to "Back to lists", lists_path %>
    <%#= hidden_field_tag :project_id, @project.id %>
  <%#= @project.inspect %>
  <%#= @project.id %>
  <%= link_to "Back to Tasks", lists_path(project_id: @project.id), class: 'btn btn-primary' %>

</div>
